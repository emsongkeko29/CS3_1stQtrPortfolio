// no script tag needed here

var age = prompt("Enter age")
var maxAge = prompt("Enter maxAge")
var numPerDay = prompt("Enter numPerDay")

let age = window.prompt("Enter ALL values for age"),
if (confirm('If ${name} are ALL the values for age')){
    console.log(totalRequired= (numPerDay*365)*(maxAge - age)
    document.write("You will need " + totalRequired + " to last until the ripe old age of " +maxAge)),
}

else {
    console.log("Try again!")
}
